version: '3.8'

services:
  mysql:
    build:
          context: ./my-context
          dockerfile: Dockerfile
    environment:
      MYSQL_ROOT_PASSWORD: password
    volumes:
      - ./mysql:/var/lib/mysql
      - ./backups:/backups
